# this is required to proxy Grafana Live WebSocket connections.
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

upstream server_registry {
    server 127.0.0.1:12929 down;
    server megaspell-server-registry:8080 backup;
}

server {
    listen 80;

    location /api/registry/actuator {
        return 404;
    }

    location /api/registry/ {
        proxy_pass http://server_registry/;
    }

    location = /robots.txt {
        add_header  Content-Type  text/plain;
        return 200 "User-agent: *\nDisallow: /\n";
    }

    location / {
        resolver 127.0.0.11 valid=30s;
        set $target megaspell-blog;
        proxy_pass http://$target$request_uri;
    }

    location /grafana/ {
        resolver 127.0.0.11 valid=30s;
        set $target grafana:3000;

        proxy_set_header Host $http_host;
        proxy_pass http://$target$request_uri;
    }

    # Proxy Grafana Live WebSocket connections.
    location /grafana/api/live/ {
        resolver 127.0.0.11 valid=30s;
        set $target grafana:3000;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_pass http://$target$request_uri;
    }
}
